[package]
name = "rsc2_macro"
version = "0.1.0"
authors = ["hyyking <leoduret@outlook.com>"]
edition = "2018"

[features]
run = []
derive = []

[lib]
proc_macro=true

[[test]]
name = "derive"
path = "../tests/derive.rs"


[dependencies]
syn = {version="1.0.*", features=["full"]}
quote = "1.0.*"
